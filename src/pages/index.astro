---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Pace Converter - Running & Swimming" description="Convert between pace, speed, time, and distance for running and swimming. Fast, mobile-first pace calculator for athletes.">
  <main>
    <header class="header">
      <div class="container">
        <h1>🏃🏊 Pace Converter</h1>
        <p class="subtitle">Convert between pace, speed, time, and distance</p>
      </div>
    </header>

    <div class="container">
      <div class="sport-selection">
        <h2>Choose your sport</h2>
        <div class="sport-cards">
          <a href={`${import.meta.env.BASE_URL}/running`} class="sport-card">
            <div class="sport-emoji">🏃</div>
            <h3>Running</h3>
            <p>Convert pace, speed, and distance for running</p>
          </a>
          <a href={`${import.meta.env.BASE_URL}/swimming`} class="sport-card">
            <div class="sport-emoji">🏊</div>
            <h3>Swimming</h3>
            <p>Convert pace, speed, and distance for swimming</p>
          </a>
          <a href={`${import.meta.env.BASE_URL}/triathlon`} class="sport-card">
            <div class="sport-emoji">🏊🚴🏃</div>
            <h3>Triathlon Timing</h3>
            <p>Estimate swim, bike, run splits and total time</p>
          </a>
        </div>
      </div>

      <footer class="footer">
        <p>Made for athletes who want to quickly convert their training metrics</p>
      </footer>
    </div>
  </main>
</Layout>

<style>
  main {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  .header {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
    color: white;
    padding: 3rem 1rem;
    text-align: center;
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  }

  .header h1 {
    margin-bottom: 0.5rem;
  }

  .subtitle {
    font-size: 1.125rem;
    opacity: 0.95;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 1rem;
    width: 100%;
  }

  .sport-selection {
    text-align: center;
    padding: 2rem 0;
  }

  .sport-selection h2 {
    font-size: clamp(1.5rem, 4vw, 2rem);
    margin-bottom: 2rem;
    color: var(--text-color);
  }

  .sport-cards {
    display: grid;
    gap: 2rem;
    max-width: 800px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .sport-cards {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .sport-card {
    background: white;
    border-radius: 1rem;
    padding: 3rem 2rem;
    box-shadow: var(--shadow);
    text-decoration: none;
    color: var(--text-color);
    transition: transform 0.2s, box-shadow 0.2s;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .sport-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 20px -5px rgb(0 0 0 / 0.2);
  }

  .sport-emoji {
    font-size: 4rem;
    line-height: 1;
  }

  .sport-card h3 {
    font-size: 1.5rem;
    margin: 0;
    color: var(--primary-color);
  }

  .sport-card p {
    margin: 0;
    color: #6b7280;
    font-size: 1rem;
  }

  .footer {
    text-align: center;
    padding: 2rem 1rem;
    color: #6b7280;
    font-size: 0.875rem;
    margin-top: auto;
  }

  @media (max-width: 640px) {
    .header {
      padding: 2rem 1rem;
    }

    .container {
      padding: 1.5rem 1rem;
    }

    .sport-card {
      padding: 2rem 1.5rem;
    }
  }
</style>
