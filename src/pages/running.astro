---
import Layout from '../layouts/Layout.astro';
import Converter from '../components/Converter.astro';
import { RUNNING_CONFIG } from '../utils/converterConfig';
---

<Layout title="Running Pace Converter" description="Convert between pace, speed, time, and distance for running. Fast, mobile-first pace calculator for runners.">
  <main>
    <header class="header">
      <div class="container">
        <h1>🏃 Running Pace Converter</h1>
        <p class="subtitle">Convert between pace, speed, time, and distance</p>
      </div>
    </header>

    <div class="container">
      <div style="text-align: center;">
        <a href={import.meta.env.BASE_URL} class="back-link">← Back to menu</a>
      </div>
      <Converter config={RUNNING_CONFIG} />

      <section class="content-section">
        <h2>How the running converter calculates results</h2>
        <p>
          The converter uses consistent unit conversions and simple physics formulas to compute pace, speed, time, and distance.
        </p>
        <ul>
          <li><strong>Pace</strong>: pace = time / distance, displayed as minutes per unit (min/km or min/mi).</li>
          <li><strong>Speed</strong>: speed = distance / time, displayed as km/h, mph, or m/s.</li>
          <li><strong>Time from pace & distance</strong>: totalSeconds = paceSecondsPerUnit × distanceInUnits.</li>
          <li><strong>Distance from pace & time</strong>: distanceInUnits = totalSeconds / paceSecondsPerUnit.</li>
        </ul>
        <p>
          Unit conversions: 1 mile = 1.60934 km, 1 km = 1000 m. Pace is formatted as mm:ss.
        </p>
      </section>

      <!-- Future content section: explanations, tables, etc. -->
      <!--
      <section class="content-section">
        <h2>Understanding Running Pace</h2>
        <p>Add explanatory text here...</p>
      </section>

      <section class="tables-section">
        <h2>Pace Reference Tables</h2>
        <p>Add pace tables for 5K, 10K, Half Marathon, Marathon...</p>
      </section>
      -->

      <footer class="footer">
        <p>Made for runners who want to quickly convert their training metrics</p>
      </footer>
    </div>
  </main>
</Layout>

<style>
  main {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  .header {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
    color: white;
    padding: 3rem 1rem;
    text-align: center;
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  }

  .header h1 {
    margin-bottom: 0.5rem;
  }

  .subtitle {
    font-size: 1.125rem;
    opacity: 0.95;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 1rem;
    width: 100%;
  }

  .back-link {
    display: inline-flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 0.75rem;
    background: white;
    color: var(--primary-color);
    text-decoration: none;
    font-size: 0.9375rem;
    font-weight: 500;
    border-radius: 0.375rem;
    box-shadow: var(--shadow);
    margin: 0 auto 2rem;
    transition: all 0.2s;
  }

  .back-link:hover {
    box-shadow: 0 4px 8px -2px rgb(0 0 0 / 0.2);
    transform: translateY(-2px);
  }

  .footer {
    text-align: center;
    padding: 2rem 1rem;
    color: #6b7280;
    font-size: 0.875rem;
    margin-top: auto;
  }

  .content-section {
    max-width: 900px;
    margin: 1rem auto 2rem;
    background: #ffffff;
    border: 1px solid var(--border-color);
    border-radius: 0.75rem;
    box-shadow: var(--shadow);
    padding: 1.5rem;
    color: var(--text-color);
  }

  .content-section h2 {
    margin-bottom: 0.75rem;
    color: var(--primary-color);
    font-size: 1.25rem;
  }

  .content-section ul {
    margin: 0.5rem 0 0;
    padding-left: 1.25rem;
  }

  @media (max-width: 640px) {
    .header {
      padding: 2rem 1rem;
    }

    .container {
      padding: 1.5rem 1rem;
    }
  }
</style>
